---
import Layout from '../layouts/Layout.astro';

import { getCollection } from 'astro:content';
const projects = await getCollection('projects', ({ data }) => !data.draft);
---

<Layout title='jonathan cheng'>
  <header
    class='flex h-screen w-full flex-col items-center justify-center gap-12 sm:flex-row sm:gap-24'
  >
    <div class='flex flex-col items-center justify-center sm:items-start'>
      <h1 class='text-4xl font-bold sm:text-5xl'>Hi! I'm Jonathan.</h1>
      <h2 class='text-2xl sm:text-3xl'>I'm a full-stack developer</h2>
      <h2 class='text-2xl sm:text-3xl'>
        and <span>a sports writer.</span>
      </h2>
    </div>
    <div class='h-48 w-48 bg-black'></div>
  </header>
  <!-- <ul>
    {
      projects.map((project) => (
        <li>
          <a href={`/projects/${project.slug}`}>{project.data.title}</a>
        </li>
      ))
    }
  </ul> -->
</Layout>
